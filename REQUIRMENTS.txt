streamlit
requests
polyline
pydeck
Pillow
python-dotenv

# Optional: powers Tab 6 (ADAS Collision Watch). App runs fine without
# these -- Tab 6 just shows a friendly "not available" message instead
# of crashing if they're missing. They're heavier installs, so only
# keep them if you want that tab working on Streamlit Cloud.
ultralytics
opencv-python-headless
